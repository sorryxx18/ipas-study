#!/usr/bin/env python3
import json, pathlib, datetime, shutil
base=pathlib.Path('/Users/leifhuang/.claude/projects/-Users-leifhuang/ipas_study')
guide_path=base/'guide_s3.json'; state_path=base/'rewrite_state.json'
backup_dir=base/'backups'/(datetime.datetime.now().strftime('%Y%m%d-%H%M%S')+'-before-rewrite-s3-61-70')
backup_dir.mkdir(parents=True, exist_ok=True)
shutil.copy2(guide_path, backup_dir/'guide_s3.json'); shutil.copy2(state_path, backup_dir/'rewrite_state.json')
guide=json.loads(guide_path.read_text(encoding='utf-8')); state=json.loads(state_path.read_text(encoding='utf-8'))
contents={
61: '''# Neural Network 類神經網路

## 1. 核心概念

**類神經網路（Neural Network）** 是由多層神經元、權重與偏置組成的模型。每一層會把輸入做加權加總，再經過激活函數產生輸出；多層堆疊後，模型就能學習複雜的非線性關係。

神經網路的直覺是把資料逐層轉換成更有用的表示。淺層可能學簡單特徵，深層可組合成較抽象的特徵。它常用於影像、語音、文字、推薦與複雜表格任務，但需要足夠資料、計算資源與正確訓練策略。

神經網路不是魔法；若資料少、標籤差、特徵錯或訓練不穩，深模型也可能過擬合或無法收斂。考試常問多層結構、權重、激活函數、非線性、過擬合與深度學習適用情境。

## 2. 考試重點

- 由輸入層、隱藏層、輸出層組成。
- 神經元做加權和加偏置，再經激活函數。
- 激活函數引入非線性。
- 多層網路可表示複雜函數。
- 訓練目標是最小化損失函數。
- 需要反向傳播與優化器更新權重。
- 容易過擬合，需正則化、Dropout、資料增強等。
- 深度學習通常資料與運算需求較高。

## 3. 名詞解釋

- **Neuron**：神經元，基本計算單元。
- **Weight**：權重，模型學習的參數。
- **Bias**：偏置，使模型可平移決策邊界。
- **Hidden Layer**：隱藏層，介於輸入與輸出之間。
- **Activation Function**：激活函數，引入非線性。
- **Forward Pass**：前向傳播，計算預測值。

## 4. 常見陷阱

**陷阱1：沒有激活函數也能靠多層學非線性**
> ❌ 誤解：線性層堆很多層就會變非線性。  
> ✅ 正確：多個線性轉換合成仍是線性，需激活函數。

**陷阱2：神經網路一定優於傳統模型**
> ❌ 誤解：任何資料都應先用深度學習。  
> ✅ 正確：小資料或需高可解釋性時，傳統模型可能更合適。

**陷阱3：層越多越好**
> ❌ 誤解：深度越深表現一定越好。  
> ✅ 正確：太深可能過擬合、梯度問題或訓練困難。

## 5. 考題怎麼問

**問法1：神經網路為何能學非線性**  
→ 因為使用激活函數與多層表示。

**問法2：模型訓練學到的是什麼**  
→ 權重與偏置。

**問法3：深度學習常見風險**  
→ 資料需求高、過擬合、訓練不穩與可解釋性較低。

## 6. 記憶口訣

- **「加權加偏置，激活變非線性」**。
- **「層層轉表示，最後做預測」**。
- **「深不一定好，資料訓練要跟上」**。

## 7. 官方指引對應線索

對應科目三深度學習原理與框架。官方指引強調神經網路架構、運算流程與訓練技巧，考試常從神經元、權重、激活函數、前向/反向傳播與訓練限制命題。''',
62: '''# Backpropagation 反向傳播

## 1. 核心概念

**反向傳播（Backpropagation）** 是訓練神經網路時計算各層參數梯度的核心演算法。前向傳播先算出預測與損失；反向傳播再從輸出層往前，把損失對每個權重的影響用鏈式法則逐層計算出來。

反向傳播本身不是優化器，而是「算梯度」的方法；算出梯度後，才由 SGD、Adam 等優化器根據梯度更新參數。沒有反向傳播，深層網路很難有效知道每一層該如何調整。

考試常問反向傳播和梯度下降的差異、鏈式法則、前向/反向流程，以及梯度消失或梯度爆炸對深層網路訓練的影響。

## 2. 考試重點

- 前向傳播計算預測與 loss。
- 反向傳播用鏈式法則計算梯度。
- 梯度表示 loss 對參數的敏感度。
- 反向傳播不是優化器。
- 優化器用梯度更新權重。
- 深層網路可能遇到梯度消失/爆炸。
- 激活函數、初始化、BatchNorm、Residual 可改善梯度問題。

## 3. 名詞解釋

- **Backpropagation**：反向傳播，逐層計算梯度。
- **Chain Rule**：鏈式法則，複合函數微分規則。
- **Gradient**：梯度，loss 對參數的導數。
- **Forward Pass**：前向傳播，從輸入算到輸出。
- **Optimizer**：優化器，依梯度更新參數。
- **Vanishing Gradient**：梯度消失。
- **Exploding Gradient**：梯度爆炸。

## 4. 常見陷阱

**陷阱1：反向傳播等於梯度下降**
> ❌ 誤解：Backpropagation 就是更新權重。  
> ✅ 正確：反向傳播算梯度，梯度下降/Adam 才更新權重。

**陷阱2：只需前向傳播就能訓練**
> ❌ 誤解：算出預測就能自動改善模型。  
> ✅ 正確：需要反向傳播知道各參數如何影響 loss。

**陷阱3：深層網路梯度一定穩定**
> ❌ 誤解：層越深資訊越多，訓練一定更好。  
> ✅ 正確：深層可能梯度消失或爆炸。

## 5. 考題怎麼問

**問法1：反向傳播使用什麼數學規則**  
→ 鏈式法則。

**問法2：反向傳播的輸出是什麼**  
→ 各層參數的梯度。

**問法3：梯度消失會造成什麼**  
→ 前面層更新很小、學習困難。

## 6. 記憶口訣

- **「前向算答案，反向算責任」**。
- **「Backprop 算梯度，Optimizer 改權重」**。
- **「鏈式法則一路往回傳」**。

## 7. 官方指引對應線索

對應科目三深度學習訓練方法。官方指引強調神經網路運算流程與訓練技巧，考試常從前向傳播、反向傳播、鏈式法則、梯度更新與梯度問題命題。''',
63: '''# Activation Functions 激活函數

## 1. 核心概念

**激活函數（Activation Function）** 是神經網路中引入非線性的關鍵。每一層若只有線性轉換，即使堆很多層，整體仍等同一個線性模型；激活函數讓神經網路能表示複雜非線性關係。

常見激活函數包括 Sigmoid、Tanh、ReLU、Leaky ReLU、Softmax。ReLU 計算簡單、能減輕梯度消失，是深度網路常用選擇；Sigmoid 常用於二元分類輸出；Softmax 常用於多類別互斥分類輸出。

不同激活函數有不同限制。Sigmoid/Tanh 在輸入很大或很小時梯度接近 0，可能造成梯度消失；ReLU 可能出現 dying ReLU，也就是神經元輸出長期為 0。

## 2. 考試重點

- 激活函數引入非線性。
- 無激活函數，多層線性仍是線性。
- ReLU 常用於隱藏層。
- Sigmoid 常用於二元分類輸出。
- Softmax 常用於多類別互斥輸出。
- Sigmoid/Tanh 容易梯度飽和。
- ReLU 可能 dying ReLU。

## 3. 名詞解釋

- **Activation Function**：激活函數。
- **ReLU**：max(0,x)，常用隱藏層激活。
- **Sigmoid**：輸出 0 到 1。
- **Tanh**：輸出 -1 到 1。
- **Softmax**：把多類別分數轉為機率分布。
- **Dying ReLU**：ReLU 長期輸出 0，神經元幾乎不更新。

## 4. 常見陷阱

**陷阱1：沒有激活函數也能學複雜非線性**
> ❌ 誤解：多層線性相乘自然變複雜。  
> ✅ 正確：線性組合仍是線性，需要激活函數。

**陷阱2：Softmax 用於多標籤分類**
> ❌ 誤解：一張圖可同時多標籤仍用 Softmax。  
> ✅ 正確：多標籤常用多個 Sigmoid；Softmax 適合互斥類別。

**陷阱3：Sigmoid 適合所有隱藏層**
> ❌ 誤解：Sigmoid 最像神經元，所以深層都用它。  
> ✅ 正確：深層常因飽和造成梯度消失，ReLU 更常用。

## 5. 考題怎麼問

**問法1：激活函數主要作用**  
→ 引入非線性。

**問法2：二元分類輸出常用什麼**  
→ Sigmoid。

**問法3：多類別互斥分類輸出常用什麼**  
→ Softmax。

## 6. 記憶口訣

- **「激活一加，線性變非線性」**。
- **「隱藏常 ReLU，二元 Sigmoid，多類 Softmax」**。
- **「Sigmoid 會飽和，ReLU 會死亡」**。

## 7. 官方指引對應線索

對應科目三深度學習原理與訓練方法。考試常從非線性、ReLU、Sigmoid、Softmax、梯度消失與輸出層設計命題。''',
64: '''# CNN 卷積神經網路

## 1. 核心概念

**CNN（Convolutional Neural Network，卷積神經網路）** 是特別擅長處理影像與空間結構資料的深度學習模型。它使用卷積核在影像上滑動，偵測局部特徵，例如邊緣、紋理、形狀，再由深層組合成更高階的物件特徵。

CNN 的關鍵優勢是局部連結與權重共享。局部連結讓模型專注於鄰近像素；權重共享讓同一個濾波器可在不同位置偵測相同特徵，減少參數量並提升平移不變性。Pooling 則常用於降低空間尺寸與增加穩定性。

## 2. 考試重點

- 適合影像、視覺與空間結構資料。
- 卷積核擷取局部特徵。
- 權重共享降低參數量。
- Pooling 降低尺寸並提升穩定性。
- 深層 CNN 由低階特徵組合高階特徵。
- 常見任務：影像分類、物件偵測、語意分割。
- 不一定適合無空間結構的資料。

## 3. 名詞解釋

- **Convolution**：卷積，濾波器滑動計算特徵圖。
- **Kernel / Filter**：卷積核，用來偵測局部模式。
- **Feature Map**：特徵圖。
- **Pooling**：池化，降低空間尺寸。
- **Stride**：步幅，卷積核每次移動距離。
- **Padding**：填補邊界以控制輸出大小。

## 4. 常見陷阱

**陷阱1：CNN 只是在圖片上全連接**
> ❌ 誤解：CNN 和 MLP 差不多，只是更深。  
> ✅ 正確：CNN 靠卷積、局部連結與權重共享擷取空間特徵。

**陷阱2：Pooling 一定越多越好**
> ❌ 誤解：多做 pooling 永遠更穩。  
> ✅ 正確：過度降維可能丟失空間細節。

**陷阱3：CNN 可完全不需資料增強**
> ❌ 誤解：CNN 自動泛化，不會過擬合。  
> ✅ 正確：影像資料常需資料增強、正則化與足夠資料。

## 5. 考題怎麼問

**問法1：影像分類常用哪種網路**  
→ CNN。

**問法2：CNN 如何降低參數量**  
→ 權重共享與局部連結。

**問法3：卷積核作用**  
→ 偵測局部特徵並產生 feature map。

## 6. 記憶口訣

- **「卷積看局部，濾波抓特徵」**。
- **「同一濾波器，到處找同樣圖樣」**。
- **「CNN 強在影像空間結構」**。

## 7. 官方指引對應線索

對應科目三深度學習與電腦視覺。考試常從卷積核、特徵圖、pooling、權重共享、影像分類與 CNN 適用場景命題。''',
65: '''# CNN 架構 LeNet / AlexNet / VGG / ResNet

## 1. 核心概念

CNN 架構的演進反映了深度學習影像模型如何從淺層走向深層。**LeNet** 是早期 CNN，常用於手寫數字辨識；**AlexNet** 以 ReLU、GPU 與 Dropout 推動深度 CNN 在 ImageNet 成功；**VGG** 使用大量小型 3×3 卷積堆疊，結構簡單規律；**ResNet** 則引入殘差連接，解決深層網路訓練困難。

ResNet 的核心是讓網路學殘差 F(x)，並透過 skip connection 把輸入 x 加回輸出。這讓梯度更容易傳遞，使非常深的網路可以訓練。

## 2. 考試重點

- LeNet：早期 CNN，手寫數字辨識。
- AlexNet：ImageNet 成功、ReLU、Dropout、GPU。
- VGG：使用多層小卷積核，架構規律但參數多。
- ResNet：殘差連接，改善深層訓練。
- Skip Connection：讓資訊與梯度更容易流動。
- 架構演進重點：更深、更穩、更有效率。

## 3. 名詞解釋

- **LeNet**：早期 CNN 架構。
- **AlexNet**：深度 CNN 里程碑架構。
- **VGG**：以 3×3 卷積堆疊聞名。
- **ResNet**：殘差網路。
- **Residual Connection**：殘差/跳接連接。
- **ImageNet**：大型影像辨識資料集。

## 4. 常見陷阱

**陷阱1：VGG 因為簡單所以參數少**
> ❌ 誤解：結構規律代表很輕量。  
> ✅ 正確：VGG 參數量大，計算成本高。

**陷阱2：ResNet 只是把層數加深**
> ❌ 誤解：ResNet 成功只因為很深。  
> ✅ 正確：關鍵是 residual/skip connection 讓深層可訓練。

**陷阱3：AlexNet 與 LeNet 沒差**
> ❌ 誤解：兩者只是 CNN 名稱不同。  
> ✅ 正確：AlexNet 使用更深架構、ReLU、Dropout、GPU，影響深度學習發展。

## 5. 考題怎麼問

**問法1：哪個架構引入殘差連接**  
→ ResNet。

**問法2：VGG 特點**  
→ 多個小型 3×3 卷積堆疊。

**問法3：AlexNet 重要性**  
→ ImageNet 成功，推動深度 CNN 熱潮。

## 6. 記憶口訣

- **「LeNet 早，AlexNet 紅，VGG 深而整齊，ResNet 加捷徑」**。
- **「ResNet 不只深，重點是 skip」**。
- **「3×3 堆疊想到 VGG」**。

## 7. 官方指引對應線索

對應科目三電腦視覺與深度學習架構。考試常從經典 CNN 架構特色、ReLU/Dropout、3×3 卷積、殘差連接與深層訓練穩定性命題。''',
66: '''# RNN 遞迴神經網路

## 1. 核心概念

**RNN（Recurrent Neural Network，遞迴神經網路）** 是用來處理序列資料的神經網路。它會在每個時間步接收輸入，同時保留前一時間步的隱藏狀態，讓模型能利用過去資訊影響目前輸出。

RNN 適合文字、語音、時間序列等有順序的資料。它可用於序列分類、序列標註、語言模型與時間序列預測。核心問題是長距離依賴難學，因為反向傳播跨很多時間步時容易梯度消失或爆炸。

## 2. 考試重點

- 適合序列資料。
- 透過 hidden state 保留歷史資訊。
- 可處理變長序列。
- 常用於文字、語音、時間序列。
- 長距離依賴困難。
- 容易梯度消失/爆炸。
- LSTM/GRU 是改良 RNN 的架構。

## 3. 名詞解釋

- **RNN**：遞迴神經網路。
- **Hidden State**：隱藏狀態，保存過去資訊。
- **Time Step**：序列中的時間步。
- **Sequence Modeling**：序列建模。
- **BPTT**：Backpropagation Through Time，時間展開反向傳播。
- **Long-term Dependency**：長距離依賴。

## 4. 常見陷阱

**陷阱1：RNN 不看順序**
> ❌ 誤解：RNN 像一般 MLP，一次看所有特徵。  
> ✅ 正確：RNN 逐步處理序列並保留狀態。

**陷阱2：RNN 完美解決長距離依賴**
> ❌ 誤解：只要是序列就用普通 RNN。  
> ✅ 正確：普通 RNN 長距離依賴困難，常用 LSTM/GRU 或 Transformer。

**陷阱3：RNN 只用於文字**
> ❌ 誤解：RNN 不能處理時間序列。  
> ✅ 正確：任何序列資料都可能使用 RNN。

## 5. 考題怎麼問

**問法1：處理序列資料的傳統神經網路**  
→ RNN。

**問法2：RNN 如何保留歷史資訊**  
→ hidden state。

**問法3：普通 RNN 的主要困難**  
→ 長距離依賴與梯度消失/爆炸。

## 6. 記憶口訣

- **「RNN 一步一步讀，狀態一路傳」**。
- **「短序列可用，長依賴吃力」**。
- **「時間展開，再反向傳播」**。

## 7. 官方指引對應線索

對應科目三深度學習與序列模型。考試常從 hidden state、序列資料、BPTT、長距離依賴、梯度問題與 LSTM/GRU 改良命題。''',
67: '''# LSTM / GRU

## 1. 核心概念

**LSTM** 與 **GRU** 是為了解決普通 RNN 難以學習長距離依賴而設計的門控循環網路。它們透過 gate 控制資訊要保留、遺忘或輸出，讓重要訊息能在較長序列中保存。

LSTM 有輸入門、遺忘門、輸出門與 cell state，結構較完整也較複雜。GRU 將結構簡化，常包含更新門與重置門，參數較少、訓練較快。兩者都比普通 RNN 更適合較長序列，但在非常長的依賴或大規模語言任務中，Transformer 常更常見。

## 2. 考試重點

- LSTM/GRU 改善 RNN 長距離依賴。
- Gate 控制資訊保留與遺忘。
- LSTM 有 cell state 與多個 gate。
- GRU 結構較簡化、參數較少。
- 適合文字、語音、時間序列。
- 不能完全避免所有梯度問題。
- Transformer 在許多長序列任務中取代 RNN 系列。

## 3. 名詞解釋

- **LSTM**：長短期記憶網路。
- **GRU**：門控循環單元。
- **Gate**：門控機制，控制資訊流。
- **Forget Gate**：遺忘門。
- **Input Gate**：輸入門。
- **Cell State**：LSTM 中保存長期資訊的狀態。
- **Update Gate**：GRU 中控制保留舊狀態的門。

## 4. 常見陷阱

**陷阱1：LSTM/GRU 和普通 RNN 完全一樣**
> ❌ 誤解：只是名稱不同。  
> ✅ 正確：LSTM/GRU 用 gate 改善長距離資訊保存。

**陷阱2：GRU 一定比 LSTM 差**
> ❌ 誤解：GRU 簡化所以必然較差。  
> ✅ 正確：GRU 參數少、訓練快，很多任務表現接近 LSTM。

**陷阱3：用了 LSTM 就不會過擬合**
> ❌ 誤解：門控結構能解決所有問題。  
> ✅ 正確：仍需正則化、資料量與適當訓練。

## 5. 考題怎麼問

**問法1：改善 RNN 長距離依賴的架構**  
→ LSTM 或 GRU。

**問法2：LSTM 的核心機制**  
→ 門控與 cell state。

**問法3：GRU 和 LSTM 差異**  
→ GRU 較簡化、參數較少。

## 6. 記憶口訣

- **「LSTM 有記憶格，GRU 較精簡」**。
- **「門控決定記什麼、忘什麼」**。
- **「長序列靠 gate，比普通 RNN 穩」**。

## 7. 官方指引對應線索

對應科目三深度學習序列模型。考試常從 RNN 限制、LSTM/GRU 門控機制、長距離依賴、時間序列與文字語音任務命題。''',
68: '''# Batch Normalization / Dropout

## 1. 核心概念

**Batch Normalization** 與 **Dropout** 是深度學習中常見的訓練穩定與正則化技巧。BatchNorm 會在訓練過程中對每個 mini-batch 的中間層輸入做標準化，再學習縮放和平移參數，使訓練更穩定、收斂更快。

**Dropout** 則是在訓練時隨機關閉部分神經元，避免模型過度依賴特定路徑，降低過擬合。推論時不隨機關閉，而是使用完整網路並依實作調整尺度。

兩者作用不同：BatchNorm 主要穩定分布與加速訓練，Dropout 主要正則化。考試常問訓練/推論行為差異，以及兩者和過擬合、收斂穩定性的關係。

## 2. 考試重點

- BatchNorm 穩定中間層分布。
- BatchNorm 可加速收斂、改善梯度流。
- Dropout 隨機關閉神經元。
- Dropout 降低過擬合。
- 訓練與推論模式不同。
- BatchNorm 推論時用移動平均統計量。
- Dropout 推論時通常關閉。

## 3. 名詞解釋

- **Batch Normalization**：批次正規化。
- **Dropout**：隨機丟棄神經元的正則化方法。
- **Mini-batch**：小批次訓練資料。
- **Regularization**：正則化，降低過擬合。
- **Inference Mode**：推論模式。
- **Moving Average**：移動平均統計量。

## 4. 常見陷阱

**陷阱1：BatchNorm 是資料前處理**
> ❌ 誤解：BatchNorm 只是在輸入資料前做標準化。  
> ✅ 正確：它通常作用於網路中間層。

**陷阱2：Dropout 推論時也隨機關閉**
> ❌ 誤解：訓練和推論完全一樣。  
> ✅ 正確：推論時通常關閉 Dropout。

**陷阱3：Dropout 用越大越好**
> ❌ 誤解：丟越多越不會過擬合。  
> ✅ 正確：過高 dropout 會欠擬合。

## 5. 考題怎麼問

**問法1：穩定深度網路中間層分布用什麼**  
→ Batch Normalization。

**問法2：訓練時隨機關閉神經元用什麼**  
→ Dropout。

**問法3：Dropout 主要解決什麼**  
→ 過擬合。

## 6. 記憶口訣

- **「BatchNorm 穩訓練，Dropout 防依賴」**。
- **「訓練會丟，推論不丟」**。
- **「中間層要穩，想到 BatchNorm」**。

## 7. 官方指引對應線索

對應科目三深度學習訓練方法。考試常從 BatchNorm、Dropout、訓練/推論差異、收斂穩定與過擬合控制命題。''',
69: '''# Transfer Learning 遷移學習

## 1. 核心概念

**遷移學習（Transfer Learning）** 是把在大型資料或相關任務上學到的模型知識，轉移到新的任務。它常用於新任務資料不足、從零訓練成本太高的情境。

常見做法是使用預訓練模型當特徵抽取器，凍結大部分底層參數，只訓練最後分類頭；或進一步微調部分/全部層。影像任務常用 ImageNet 預訓練 CNN，文字任務常用預訓練語言模型。

遷移學習是否有效取決於來源任務和目標任務相似程度。若兩者差異太大，可能出現負遷移，讓表現變差。

## 2. 考試重點

- 利用預訓練模型知識。
- 適合資料少或訓練成本高的新任務。
- 可作為特徵抽取器。
- 可凍結部分層，只訓練新頭部。
- 任務相似度越高越有利。
- 可能發生負遷移。
- 常見於影像與 NLP。

## 3. 名詞解釋

- **Transfer Learning**：遷移學習。
- **Pretrained Model**：預訓練模型。
- **Feature Extractor**：特徵抽取器。
- **Freeze Layers**：凍結層，不更新其參數。
- **Target Task**：目標任務。
- **Negative Transfer**：負遷移，遷移反而降低表現。

## 4. 常見陷阱

**陷阱1：來源任務完全無關也一定有效**
> ❌ 誤解：任何預訓練模型都能幫任何任務。  
> ✅ 正確：任務差異太大可能負遷移。

**陷阱2：遷移學習等於不用訓練**
> ❌ 誤解：拿預訓練模型就直接完成。  
> ✅ 正確：通常仍需訓練分類頭或微調。

**陷阱3：資料很多也一定要遷移**
> ❌ 誤解：所有任務都必須用 transfer learning。  
> ✅ 正確：資料非常充足且任務特殊時，可從頭訓練或比較。

## 5. 考題怎麼問

**問法1：資料少但有相關預訓練模型怎麼辦**  
→ 用遷移學習。

**問法2：凍結前面層、只訓練最後分類器是什麼**  
→ 特徵抽取式遷移學習。

**問法3：來源和目標差太多會怎樣**  
→ 可能負遷移。

## 6. 記憶口訣

- **「先學大任務，再轉小任務」**。
- **「資料少，借預訓練」**。
- **「任務相近才好轉」**。

## 7. 官方指引對應線索

對應科目三深度學習訓練與模型應用。考試常從預訓練模型、特徵抽取、凍結層、資料不足、任務相似度與負遷移命題。''',
70: '''# Fine-tuning 微調

## 1. 核心概念

**Fine-tuning（微調）** 是在預訓練模型基礎上，使用目標任務或領域資料繼續訓練，使模型更適合特定任務。它是遷移學習的一種常見形式，但比單純特徵抽取更進一步，因為會更新模型部分或全部參數。

微調策略可分為只訓練輸出頭、解凍後幾層、或全模型微調。資料少時通常先凍結大部分層，避免過擬合；資料較多或領域差異較大時，可逐步解凍更多層。微調常使用較小學習率，避免破壞預訓練知識。

考試常問 fine-tuning 和 transfer learning 的關係、凍結/解凍策略、學習率設定、資料量與過擬合風險。

## 2. 考試重點

- 在預訓練模型上繼續訓練。
- 用目標任務資料調整模型。
- 可只訓練 head，也可解凍部分或全部層。
- 常用較小 learning rate。
- 資料少時需防過擬合。
- 領域差異大時可能需解凍更多層。
- 需驗證集監控與 early stopping。

## 3. 名詞解釋

- **Fine-tuning**：微調預訓練模型。
- **Head**：模型最後任務輸出層。
- **Freeze / Unfreeze**：凍結或解凍參數。
- **Learning Rate**：學習率。
- **Catastrophic Forgetting**：災難性遺忘，微調破壞原有能力。
- **Domain Adaptation**：領域適應。

## 4. 常見陷阱

**陷阱1：微調一定全模型一起訓練**
> ❌ 誤解：fine-tuning 就是所有層都大幅更新。  
> ✅ 正確：可依資料量凍結部分層，只訓練 head 或後幾層。

**陷阱2：學習率和從頭訓練一樣大**
> ❌ 誤解：微調用大學習率更快。  
> ✅ 正確：常用較小 learning rate，避免破壞預訓練表示。

**陷阱3：資料少也不會過擬合**
> ❌ 誤解：預訓練模型已很強，不會過擬合。  
> ✅ 正確：小資料微調仍可能過擬合，需正則化與早停。

## 5. 考題怎麼問

**問法1：在預訓練模型上用新資料繼續訓練叫什麼**  
→ Fine-tuning。

**問法2：資料少時微調策略**  
→ 凍結大部分層，只訓練 head 或少數後層。

**問法3：微調學習率通常如何設定**  
→ 較小，避免破壞預訓練權重。

## 6. 記憶口訣

- **「預訓練打底，微調貼任務」**。
- **「資料少少解凍，學習率小小走」**。
- **「凍結保知識，解凍學領域」**。

## 7. 官方指引對應線索

對應科目三深度學習訓練與遷移學習。考試常從預訓練模型、微調策略、凍結層、學習率、資料量、過擬合與遷移學習差異命題。'''
}
for seg in guide['segments']:
    if seg.get('id') in contents:
        seg['content']=contents[seg['id']]
        seg['round']=max(int(seg.get('round',0) or 0),2)
s3=state.setdefault('s3',{})
comp=set(s3.get('completed',[])); fail=set(s3.get('failed',[]))
for i in contents: comp.add(i); fail.discard(i)
s3['completed']=sorted(comp); s3['failed']=sorted(fail)
ft={str(k):v for k,v in s3.get('failedTitles',{}).items()}
for i in contents: ft.pop(str(i),None)
s3['failedTitles']=ft
state['lastUpdated']=datetime.datetime.now(datetime.timezone.utc).isoformat(timespec='milliseconds').replace('+00:00','Z')
guide_path.write_text(json.dumps(guide,ensure_ascii=False,indent=2),encoding='utf-8')
state_path.write_text(json.dumps(state,ensure_ascii=False,indent=2),encoding='utf-8')
print(f'backup: {backup_dir}')
print('updated S3 ids 61-70 directly by Hermes; no Claude Code subprocess used')
